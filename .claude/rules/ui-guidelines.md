# UI Guidelines(业务语义手册)

> **本文件 = 前端业务语义手册(v2 重写,2026-08-01,GH #122,spec 0018 / ADR 0015):** 只承载业务语义——显示层三态词表与映射接口、降级成因副词表、语义色映射关系(引用 DESIGN.md 令牌名)、防作假约定、分享面信息边界、成本中性化、告警事件词表。视觉与布局权威在 [DESIGN.md](../../DESIGN.md)(令牌/字阶/圆角/阴影/布局/动效/品牌);页面与组件构成规格在 surface briefs(`web/.impeccable/surfaces/`);产品形态与读者模型在 [PRODUCT.md](../../PRODUCT.md)。三处均由 `plan` agent 维护,`check` agent 按多源验收。
> **旧「信号墙」体系正式作废:** 四态中文词表(正常/降级/宕机/告警)的显示层用法、teal 色板、闪烁独占语义、AppHeader/PublicFooter/HealthBanner/EndpointCard/UptimeStrip/OverviewGroupSection/EndpointQuickViewDialog/EndpointUptimePanel/LatencySparkline 全部退役,历史仅存于 git;本文件按建成世界重写(ground truth over intention——规格与代码冲突时以代码为准并注明)。
> **不动的保留项(重写不得丢):** 后端域模型四态与告警管线(W5)、告警事件词表(含借字例外)、防作假语义全套(统计范围/null 不冒充/半成品不外流/断点不显涨跌/判分不完整水印/成本中性化)、分享面信息边界(ticket 54)。
> **语义映射(状态色、词表)变更仍属承重语义变更,须在设计评审中说明理由。**

## 1. 产品形态与读者

> 以 `PRODUCT.md` 为准——双形态(公开侧:状态概览 `/`、Benchmark `/benchmark`、端点详情 `/endpoints/:id`、故障记录 `/alerts`——四类故障叙事事件对匿名公开,边界见附录第 16 项,无需登录;管理台:评估中心 `/eval`、模型管理 `/models`、系统设置 `/settings`,需登录)、三类读者、桌面优先、Apple 风极简基调。v2 IA 映射与壳外页面(/login、/report/:token 走 `meta.bare`)见 spec 0018 与 DESIGN.md Layout 节。

## 2. 视觉基线

> 视觉规格(令牌刻度/字阶/圆角/阴影/轻容器语法/动效/暗色后置)以 `DESIGN.md` 为准。本节只登记修改纪律与暗色键位事实。

- **三层令牌架构(保留):** `tokens.css`(原始刻度)→ `semantics.css`(语义令牌,页面唯一消费层)→ `ep-theme.css`(EP 映射,全仓唯一允许书写 `--el-*` 的文件)。调具体色值改 tokens.css;调 EP 组件观感改 ep-theme.css;**页面/组件只消费 semantics.css 的语义令牌,禁止直接引用原始刻度、禁止写 `--el-*`、禁止硬编码色值**。豁免:① `--el-card-padding` 管理台密度档机制;② BrandMark 渐变 stop 表现属性(图形标识非语义表达);③ `utils/chartColors.ts` ECharts 色板 JS 镜像(§3)。引入顺序(main.ts 实际,a41bc7e 起 EP 样式为 base.css + 按需组件样式,非整包 index.css):element-plus base/组件样式 → element-plus dark/css-vars.css → tokens.css → semantics.css → ep-theme.css → print.css。**dark/css-vars.css 引入保留**(ep-theme.css 的 html.dark 块以其为级联前提),暗色后置由 boot 时 scrub(html.dark class 移除 + hs:dark 删除,GH #112)承担,不是删除引入。
- **EP 服务范围(决策 8):** Element Plus 保留管理台复杂件(表格/弹窗/表单/分页/标签页);签名面自造(AppTopbar/AppSidebar/StatusHero/MetricWidgets/ModelStatusList/UptimeMicroStrip/ModelDetailPanel/时间线),自造面不引入 EP 布局组件(AppTopbar 用户 chip 的 el-dropdown 是交互件非布局件,GH #135 票面登记)。
- **暗色后置(决策 10,登记为事实不是删除概念):** 当前恒亮主题;v1 亮/暗切换(utils/theme.ts、`hs:dark` 键、AppHeader 切换按钮、index.html 防闪脚本)已随旧外壳退役。**暗色键位预留清单**(暗色 spec 的填值起点):semantics.css `html.dark` 全键(值暂沿亮值,`@media screen` 作用域)+ chartColors.ts `CHART_COLORS_DARK` 全字段(暂镜像 LIGHT)+ tokens.css 阴影暗色覆盖键位 + index.html 防闪脚本恢复路径。暗色 spec 落地时只改语义层与镜像,页面零改动。
- **间距与密度:** 4px 基准网格消费 `--hs-space-1..9`;公开页 32–48px 页面留白,管理台页 24/16;轻容器内边距桌面 24/32、窄屏 16;管理台 EP 卡片紧凑档 12px 走 `--el-card-padding`,禁 `:deep(.el-card__body)` 覆写。导出物料(StatusCard/EvalCard 独立画布)不受密度档约束,画布边距是物料设计常量(share-materials brief 登记)。
- **z-index / 过渡:** z-index 八档刻度见 DESIGN.md(页面代码禁刻度外字面量);过渡两档(default 0.2s 反馈 / slow 0.3s 页面切换),动效体系见 §3 动效节与 DESIGN.md Motion 节。

## 2b. 品牌标识(BrandMark / Wordmark)

- **BrandMark 是唯一图形标:** 瞄准镜字形(圆环 + 十字准星刻度 + 中心脉冲点,监控隐喻),**蓝渐变 blue-400 #549CFF → blue-700 #0062CC**(v2 重建,ADR 0015;与 ProxyHub 家族同源关系解除,PRODUCT.md 品牌承诺段)。`<stop>` 双写纪律沿置:`stop-color` 表现属性(#549cff / #0062cc)是 snapdom 捕获兜底,`var()` 令牌链是浏览器内权威;tokens.css 改色两处必须同步(BrandMark.vue 注释与本条互指)。em 尺寸宿主可控(宿主 font-size 定档:AppTopbar 24 / LoginView 40 / 物料品牌区按画布规格——detector 豁免登记在 .impeccable/config.json)。
- **Wordmark 是唯一字标:** 「HubScope」PascalCase + 品牌蓝脉冲小圆点(常亮,非动画),系统等宽字栈,字重 700,em 基准宿主缩放。
- BrandMark 永不裸用,永远与 Wordmark 同场出现(AppTopbar 品牌块、LoginView 品牌区、物料品牌区)。

## 3. 语义色映射(核心约定)

颜色承载业务语义,**映射关系只在本文件定义**,组件引用同一映射,禁止各组件自造颜色语义。文字/图形双阶分工是 v2 的底座纪律:**文字场景一律消费 `--hs-*-text` 阶(实测过 AA),图形填充消费本体阶**(DESIGN.md Graphic-Text Division Rule)。

### 3.1 显示层三态(endpoint 状态域,核心)

- **域模型四态保留(W5 不动):** healthy / degraded / down / failing 仍是后端状态机、Lark 告警与告警历史的语言。
- **显示层三态(v2 决策 3;词表经 GH #128 切参照稿,2026-08-01 用户裁决;2026-08-02 稳定运行简化):** **稳定**(绿)/ **降级**(黄)/ **异常**(红)——failing 在显示层并入异常,无独立显示身份;状态呈现统一为「小圆点 + 状态词」,局部强调而非大面积色块。Stable / Degraded / Incident 为英文译名档案(spec 0018 §11),界面中文主显。
- **唯一映射点 = `utils/statusDisplay.ts`(接口登记,GH #113 裁决):** `statusDisplay(status, causes?) → { status, label, tone, causeSuffix }`、`statusLabel`、`statusTone`、`toDisplayStatus`、`displayStatusCounts`(down+failing 合并计数)、`DISPLAY_SEVERITY_ORDER`(重→轻:incident/degraded/stable)。色槽是抽象槽(success/warning/danger),组件按通道取令牌:**词 → `*-text` 阶,点 → 本体阶**。未知输入 → 「未知」+ warning 槽(中性回落:既不读作稳定也不读作异常)。组件内禁写状态词字面量;告警历史事件词表(§7)不经过本映射。
- **降级成因副标签保留(spec 0013):** degraded 显示态可挂「· 可用性」「· 延迟」副标签(causeSuffix;双命中「· 可用性 + 延迟」,顺序与后端 `degrade_causes` 一致);副标签是 Badge 文字的一部分,无独立圆点/底色,secondary 不随词着色;聚合场景(hero、分区头、计数)永不传 causes——成因是端点粒度信息。
- **状态筛选跟显示词表走(GH #113 main 裁决;词随 GH #128 更新):** 筛选器三选项(稳定/降级/异常),「异常」同筛 down+failing;UI 不提供 failing 单筛。
- **物料分布串三段(GH #113 main 裁决;词随 GH #128 更新):** 「稳定 N · 降级 N · 异常 N」恒列三段;failing 计数由「含 N 个告警」事件 chip 单独披露——异常不掩盖(防作假不回退),词表统一。
- **同一语义在公开侧与管理台必须同色同词**——管理台表格中的状态展示与状态概览同一三态。

### 3.2 状态色映射表

| 业务语义 | 点/图形(本体阶) | 词/文字(*-text 阶) | 说明 |
|---|---|---|---|
| 稳定 | `--hs-success` #34C759 | `--hs-success-text` #1F7A33 | 显示态 |
| 降级 | `--hs-warning` #FF9500 | `--hs-warning-text` #B35C00 | 显示态;成因副标签 secondary |
| 异常 | `--hs-danger` #FF3B30 | `--hs-danger-text` #D70015 | 显示态(down+failing 合并) |
| 评分/百分比档位 | 绿/黄/红阈值 | 同左文字阶 | 阈值以后端口径为准,前端不自定分界线 |

- **failing 的显示层残留(仅两处,均为 danger 槽):** ① 物料「含 N 个告警」事件 chip(danger-text 描边 chip + danger 实心点,事件词非状态词);② 告警历史词表「厂商组告警」(§7 借字例外)。`--hs-status-failing` 令牌值为 danger 红,混合期兼容保留,新世界零消费,待物理删除。
- **24h 可用率三档着色(保留,状态概览/物料/详情页共用):** ≥95% success 槽、<95% warning 槽、0%(有探测且全失败)danger 槽、无探测数据 `--hs-border` 灰(图形)/ `--hs-text-placeholder`(文字);集中 `utils/overviewMetrics.ts` 的 `availabilityRateTier` 与 `utils/overviewDots.ts` 的 `dotTier`,文字场景走 *-text 阶。聚合口径 = 按小时对齐求和 total/failures(探测加权),禁止按端点简单平均。
- **分数涨跌指示(保留):** 升 = success-text、降 = danger-text;持平、无上批次、**跨 Suite 版本断点(分数不可比)一律不显示涨跌箭头**,用 `--hs-text-placeholder` 占位并标注「题目已变更」(ADR 0007 防作假核心语义:禁止把题库变化呈现为模型降级)。
- **榜单条形(0–100,保留):** 按评分档位阈值着色(绿/黄/红,阈值以后端口径为准),条刻度恒 0–100(W7 绝对分制的视觉镜像),不做行内/列内归一化;档色同时承担维度格子与总分条的强弱表达。
- **批次/运行状态色映射(GH #120 定稿口径:词 *-text 阶、点图形基色):**

| 业务语义 | 词(*-text 阶) | 点(基色) | 说明 |
|---|---|---|---|
| 运行中 | `--hs-brand` | `--hs-brand` | 进行中强调;禁闪烁(闪烁语义已整体退役);禁用 warning 黄(黄 = 降级专属) |
| 等待中 | `--hs-text-placeholder` | `--hs-text-placeholder` | 未开始,中性 |
| 已完成 | `--hs-success-text` | `--hs-success` | 与 endpoint 域同向(好) |
| 失败 | `--hs-danger-text` | `--hs-danger` | 与 endpoint 域同向(坏) |

- **ECharts 色板 JS 镜像(`utils/chartColors.ts` 单一来源):** `CHART_COLORS_LIGHT` 与 semantics.css 亮值逐一同步;`CHART_COLORS_DARK` 全字段预留(暂镜像 LIGHT,暗色 spec 填值);`failing` 字段 = danger 红(显示层合并,字段保留 seriesPalette 五槽结构);`bgHover` 字段 = 面积填充镜像(功能性强调非装饰,实心禁渐变)。`useChartColors` 恒供 LIGHT,ComputedRef 签名保留(暗色 spec 一处恢复分叉)。**令牌值变更登记必须附 chartColors 镜像受影响字段清单**(check 按清单逐字段验收)。
- **静态导出物料(PNG)中的语义表达(批 56 约定,v2 修订):** 动画不可用时,状态表达转译为着色文字——三态词按 *-text 阶着色;failing 维度由「含 N 个告警」danger 事件 chip 披露(显示层无第四色,**禁止静默省略 failing 维度**;物料分布串三段恒列,见 §3.1)。静态物料无 hover,长文本截断长度按完整可读性折中,不依赖 `title` 全显。
- **判分不完整/半成品等评估域语义:** 见 §5 Leaderboard 条目,全部保留不变。

### 3.3 图表纪律(Apple Health 化 + 防作假保留,GH #114 登记)

- **平滑曲线 = Fritsch–Carlson 单调三次插值,唯一来源 `utils/monotoneSmooth.ts`**(局部极值点强制零切线 + α²+β²≤9 限量;采样点恒落本段两端点值之间,不发明极值;插值性保证逐点穿过实测点;vitest 17 用例)。否掉 ECharts 内置 smooth(cardinal 样条,尖峰邻域过冲)。
- **ProbeLatencyChart 不平滑(偏离登记,GH #114):** 逐探测点曲线保留原始形态——① item-trigger tooltip 会把插值点呈现为探测读数(保真红线);② 逐探测尖峰是事故证据,平滑会削弱。若未来裁决逐探测曲线也要平滑,需先解决插值点禁 hover,另立小票。
- **样式口径:** 线宽 2、无逐点圆点(TrendChart 实测点保留 5px 极简点)、y 轴无线无刻度 + 虚线水平网格 splitNumber 4、x 轴标签稀疏仅落实测点、tooltip 吸附最近实测槽报真值(保真值不随量程截断);少坐标少网格,注意力在趋势。
- **防作假保留:** null 断线(无数据处不连线,「无证据」不被伪造);x 轴恒时间轴(category 轴会把不均匀探测间隔呈现为均匀,伪造证据形态);失败探测延迟永不进 y 量程。
- **入场动画:** 1000ms(800–1200 档中值)经 `utils/chartMotion.ts` 门控,reduced-motion 归零;30 天桶(5761 点)超 ECharts animationThreshold 2000 自动关闭(已登记)。
- **断线纪律(详情面板延迟趋势,GH #114 沿置):** 连续成功探测时间间隔 > 3×中位间隔处断线;失败表达 = 故障窗浅色带(markArea,相邻失败间隔 ≤ 2×中位间隔合并,danger 浅底整高;单点故障窗渲染边界向两侧各扩 0.5×中位间隔保可见,真实边界不动);面积填充 `--hs-bg-hover` 实心(chartColors `bgHover` 镜像);y 量程 = max(P99×1.25, 1000ms 下限)仅成功探测,成功样本 <20 回退峰值;超界点自然裁剪,禁视觉钳制。

### 3.4 动效体系(v2.0 §15 落地版)

视觉规格在 DESIGN.md Motion 节;语义层约定:

- **禁闪烁(ADR 0015):** 「闪烁 = failing 告警专属、全站唯一动画」整条退役;全站无任何闪烁动画。告警紧急度显示层表达 = danger 双编码 + 物料事件 chip。
- **动效词汇表(封闭):** 数字补间(500–800ms,仅健康指数与指标区核心数字,`utils/numberTween.ts`)/ 图表入场绘制(800–1200ms,一次性,`utils/chartMotion.ts`)/ 页面 Fade + ≤10px 位移(200–300ms,App.vue slow 档)/ hover 上浮 2–4px + shadow-md(**卡片类可点元素;表格/矩阵行豁免**,GH #118 裁决——hairline 行上浮破坏共享 grid 基线与仪式竖条几何,行可点性由 hover 填充 `--hs-brand-soft` + 指针承担)/ 局部刷新(轮询只更新变化区域)。
- **状态点呼吸(未落地,登记为已知差距):** spec §15 的呼吸动效尚无实现(StatusBadge 注释登记为后续动效票);状态变化当前走 `--hs-transition` 颜色过渡。
- **常驻动画唯一例:** AppSidebar 批次入口 Loading 图标 1s 线性旋转(工具性进度指示,组件级 reduced-motion 门控)。
- **reduced-motion(WCAG 2.3.3,Gate-The-Phases 平移保留):** CSS 过渡由 semantics.css 全局 `transition: none !important` 一处归零;JS 阶段时序(补间/图表绘制/旋转)单独 matchMedia 门控,延迟归零、终态直呈。

## 4. 布局规范

> 布局与密度档(1200px、轻容器、留白、1024 断点)以 `DESIGN.md` 为准;本节保留工程行为约定。

- 内容容器首选轻容器语法(自造)或 `el-card`(管理台);列表数据首选 `el-table`(管理台);多功能分区用 `el-tabs`(**不加 `lazy`**,保轮询事件,ticket 19)。**评估中心二级 tab 定稿(GH #120):榜单 / 评估运营 / 题库,query 值 board/ops/cases 冻结为稳定深链锚点(legacy /admin 重定向落点),不改名。
- 卡片内容**不得溢出、不得出现横向滚动条**;弹性列宽优先于固定宽度。管理台 `el-table` 的弹性内容列必须显式 `min-width`(AuditLogs 先例),使全表最小宽度可算术核对。窄屏处置 = 形态切换,不是横滚豁免(1024 断点,DESIGN.md Layout)。
- **管理台表单控件限宽档(ticket 102 登记,v2 平移):** 长输入档 560px(URL/webhook/token 类);标准输入档 320px(标识/短文本);`el-input-number`/`el-switch`/`el-select` 用组件默认宽度。输入框 + 行内操作按钮必须同行(flex,输入框 `flex: 0 1 auto` 限宽、按钮 `flex: none`);label 宽度 ≤4 字 90px、≤6 字 120px;多值紧凑排布走 `flex-wrap` 横排,禁每值独占一行竖排堆叠。公开消费页表单(登录页等)自布局,不受此限。
- **页面标题惯例(GH #118 裁决,T13 登记):** 页面 h1 文案 = 侧边栏标签(状态概览/Benchmark/评估中心/模型管理/故障记录/系统设置);「评估榜单」等描述性短语只作 lede/副标题。深链页(批次报告、端点详情)h1 为对象名(批次号/模型名),不受此惯例管辖。字号档:一级页面 h1 一律 3xl 页面标题档(2026-08-01 main 裁决落地:模型管理/系统设置两页自 xl 升 3xl,与 spec 0018 §14 对齐)。**例外已退役(2026-08-01,参照稿复刻批):** DashboardView 原 sr-only h1「HubScope 服务状态总览」由可见页头(h1「状态概览」+ lede「全局视角,掌握 AI 服务运行健康状态」md secondary)取代——惯例自此全无例外,可见 h1 直接承担 a11y 树(dashboard surface brief 同步登记)。
- **跨组件样式覆盖纪律(GH #121 check HIGH-1 沉淀,T13 登记):** 子组件 scoped 样式内 `:deep(.祖先类)` 是**构造性死选择器**——scoped 作用域 ID 只从父向子附加,子组件选择器永远匹配不到祖先元素上的类。跨组件变体覆盖只允许两条路:① 子组件声明 prop(如 `compact`),把变体类绑在自身根元素;② 父组件写 `.ancestor :deep(.child)`(父的作用域 ID 在祖先元素上,选择器成立)。check 按本条拦截第三写法。

## 5. 组件使用规范

- **Element Plus 复杂件优先(管理台),签名面自造(决策 8);不引入新 UI 库,不自造表单/表格/分页。**
- **StatusBadge 是唯一的状态展示组件(三态重建,GH #113):** 「小圆点 + 状态词」双编码——点 9px(sm)/11px(md)本体阶,词 `--hs-text-sm`(md 档 `--hs-text-md`/600)*-text 阶;词与色槽全部来自 `utils/statusDisplay.ts`,禁写状态词字面量;可选 prop `causes` 挂降级成因副标签(§3.1)——**消费方收窄(2026-08-02 用户裁决):ModelStatusList 行不再传 causes(状态列收窄 100),成因副标签存续于详情面板等分诊面**;可选 prop `reason` 经 `title` 提供判定依据;size 'sm'|'md'。**dotless prop 存续但当前零消费方**(v2 列表/物料场景均由「点 + 词」同场承担;旧 dotless 三处封闭清单随信号墙退役)——保留为可用变体,新增消费方须设计评审登记。零动画(闪烁退役);状态变化走 `--hs-transition` 颜色过渡。需要展示 endpoint 状态处一律复用,禁止第二个状态灯实现。
- **AppTopbar 是全宽顶栏唯一组件(GH #135,参照稿复刻 Q1——顶部 Header 恢复):** 56px 白面 + 底部 hairline,sticky top 0(z-sticky);左 = 品牌块(BrandMark 24 + Wordmark lg,router-link 回 `/`,品牌位自侧栏迁入——侧栏不再承担品牌位);右 = 登录态:铃铛图标按钮(lucide bell 18px,**纯导航入口** → /alerts,无未读/批次角标语义)+ 用户 chip(circle-user-round 20px + 用户名截断 + `roleLabel` 纯文本角色,el-dropdown 单项「退出」(log-out 图标)——EP dropdown 自带 focus/ESC/方向键,自造模态三件套不适用;**退出口径自侧栏账号行迁入**,失败保留会话 UI + ElMessage 原因;**退出成功 dispatch `hs:auth-changed` 窗口事件**(2026-08-02,见 AppSidebar 条事件通道));未登录 = 右侧不再渲染登录入口(2026-08-02 用户裁决:「管理登录」迁侧栏底部用户槽位,推翻 ticket 90「登录入口统一在顶栏」口径——顶栏匿名右侧留白)。会话状态 mount + 路由切换重检(refreshAuth watch 先例),无状态库。**窄屏(2026-08-01 外壳抽屉批):** <1024px 左端渲染汉堡按钮(lucide menu 18px,抽屉开时换 x,aria-expanded,aria-label「打开/关闭导航菜单」,`data-drawer-toggle` = 焦点归还锚点),桌面 `display: none`;窄屏顶栏 padding 收紧 space-3。**Header 高 56px 与 AppSidebar sticky top/height 互指,改动必须同步;`meta.bare` 壳外页(/login、/report/:token)连 Header 也不渲染。**
- **AppSidebar 是全站外壳唯一组件(GH #112;GH #135 复刻批改造;GH #139 灰底白卡 → GH #148 白底灰 tile):** 220px 自造侧边栏,位于 AppTopbar 下方(sticky top 56px、height calc(100vh − 56px),与顶栏高度互指),**底色 = `--hs-bg-card` 白(2026-08-02 用户裁决,推翻 GH #139「侧栏与 main 区同底灰」——同底无分栏感:侧栏白与 AppTopbar 连成白色轨道,main 区保持 `--hs-bg-subtle` 浅灰骨架底 + 白 tile 分层)**,构成自上而下——导航项(`utils/sidebarNav.ts` 集中:六项 IA 词表、登录态过滤 `visibleSidebarItems`、激活匹配 `isSidebarItemActive`,未登录只见公开三项——状态概览/Benchmark/故障记录(附录第 16 项公开化);**图标 = Lucide 风格内联 SVG 集 `components/icons/lucide.ts`**(24 viewBox、stroke=currentColor、stroke-width 2、round caps,渲染 18px):状态概览=activity、模型管理=atom、评估中心=clipboard-list、Benchmark=file-text、故障记录=triangle-alert、系统设置=settings,**键集与 sidebarNav 一一对应(vitest 守卫)**;项高 48px、项间距 space-2、字号 md、radius-lg;**激活 = 软蓝 pill(brand-soft 底 + brand 词与图标)**,未激活 text-regular + 图标 secondary,hover bg-hover)→ 批次进度入口(登录态 + 存在未完成批次时渲染,文案「批次运行中 X/Y」,点击深链 `/eval?batch=N`;3s visibilityPoll,settle 即停即隐,卸载清理;ticket 52 逻辑自 AppHeader 迁入)→ 底部(GH #139 三段:**登录态用户卡 = 灰 tile**(`--hs-bg-subtle` + 1px 描边 + radius-lg + padding space-2/space-3,白侧栏上保持层次——2026-08-02 随侧栏白底修订;36px 圆形头像位 = circle-user-round 线性图标 on bg-card + 右下角 8px success 实色在线点(tile 底色描边)、用户名 md/600 截断、`roleLabel` xs secondary 纯文本角色;**退出已迁 AppTopbar 用户 chip,侧栏不再放退出**;**未登录 = 同槽位渲染登录入口**(灰 tile + circle-user-round 图标 +「管理登录」→ /login——2026-08-02 用户裁决,入口自顶栏迁入,推翻 ticket 90「登录入口统一在顶栏」口径)→ **hairline 一条**(自 side-footer 盒顶移至法律行顶)→ **法律单行**「© 2026 HubScope · vX.Y.Z」(xs placeholder,版权与版本号同一行,GH #122 恢复登记存续:PublicFooter 退役时 spec 0018 未载有意去除,保守恢复;版本号 mono,title 全版本,获取失败静默省略版本段;**版本短显口径(2026-08-02 裁决,显示层唯一改动):** release 串取 `^v\d+\.\d+\.\d+` 前缀,`dev-` 开头构建串显 `dev-<短哈希>`(如 dev-gfe91a9d;无哈希可提取时如实回显 describe 主体,不发明哈希——check #146 LOW-1 登记),title 恒留完整串——部署脚本版本戳不动)。**side-footer 钉底(2026-08-02 用户裁决):** aside 高度 calc(100vh − 56px) 内导航区 flex 1 自滚动,side-footer flex none——用户卡/登录入口与法律行恒可见,不随内容超高滚出。会话状态 mount + 路由切换重检(refreshAuth watch 先例)+ **`hs:auth-changed` 窗口事件通道(2026-08-02;常量与 dispatch helper 集中 `utils/authEvents.ts`):** 登录/退出成功后由 LoginView/AppTopbar dispatch;监听方 = AppSidebar(refreshShellState)/ DashboardView(authed owner——RecentEvents 经 prop 间接收敛,不立第二会话源,main 裁决)/ AlertsView(refreshAuth),挂载/卸载配对清理——修复「退出后 router.push('/') 无路由变化时侧栏菜单停登录态」实机缺陷;无状态库沿置。**窄屏抽屉形态(2026-08-01 外壳抽屉批):** <1024px 侧栏退出 flex 行,fixed 于顶栏之下(top 56px 与顶栏高互指,宽 260px,`--hs-z-drawer` + 开时 shadow-lg,抽屉面底色 `--hs-bg-card` 白,与桌面侧栏同色(2026-08-02 侧栏白底后自然一致;压灰 scrim 读感不变)),关闭 = translateX(-105%) + visibility hidden(出 a11y 树与 tab 序),开 = 滑入;scrim 覆盖顶栏以下(`--hs-z-overlay` + `--hs-overlay-bg`,fade);抽屉状态由 App.vue 持有(汉堡开合、路由切换自动关、回桌面强制关),关闭三路统一(ESC / scrim / 导航选择)+ focus trap(utils/focusTrap.ts,开时焦点落首个导航项)+ 焦点归还汉堡(App.vue 经 `data-drawer-toggle`;**焦点归还只覆盖 ESC/scrim 两路,导航选择关闭后焦点随页面切换、不归还汉堡**——有意偏差登记:导航的目的地是新页面)——自造模态面三件套沿置;桌面形态逐像素不变。
- **StatusHero 是状态概览健康指数区的唯一组件(GH #115;GH #139 白 tile 化):** **区域容器 = 白 tile**(bg-card + 1px 描边 + radius-lg,内 padding 24 纵 / 32 横,灰骨架底上的区域白卡,GH #139;区间距 space-4 节奏),hero 72px 大数字(健康指数,后端聚合直渲,`formatPercentDigits` + tabular-nums)+ 次级「%」(2xl)+ 结论词(xl/600,tone 着色走 *-text 阶,措辞与物料同源 `utils/healthConclusion.ts`)+ 日环比行(`healthDeltaText`,null 整行不渲染——不发明「持平」)+ 统计范围注记(`heroScopeText`「统计范围:N 个启用端点」,xs/secondary)。**防作假不变式:** 健康指数 null → 中性「暂无数据」(3xl placeholder,永不显示 100%);null 折叠进 empty 分支,结论词不读作「全部稳定」。数字补间 500–800ms(`useTweenedNumber`);skeleton 与加载态同高锚定(242px 下限,算术注释在组件内)。
- **MetricWidgets 是指标区唯一组件(GH #115):** 四格 Apple Widget 风轻容器(白面 + 1px 描边 + radius-lg;hover 上浮 2px + shadow-md——卡片类可点感,非表格行)——24h 可用率(副行 = 日环比,与健康指数同口径同源;null 显「较昨日暂无对比」)/ 24h 请求量(探测总次数,后端聚合)/ 平均延迟(副行恒注口径「启用端点 P50 均值」,批 59 scope 恒一致口径)/ 风险模型数(副行 = 「异常 N · 降级 N」或「全部稳定」,模型级去重口径见 overview brief;伞状标题不撞状态词「异常」,GH #128)。核心数字 3xl/600 tabular-nums 补间;每格配 `TrendSparkline` 轻趋势线(单调插值、null 断线、无轴无网格、中性墨色,永不与核心数字争层级);标量一律后端聚合直渲或登记口径纯函数,组件不推导。
- **ModelStatusList 是模型状态列表唯一组件(GH #115,取代 EndpointCard 卡片矩阵;GH #136 七修;GH #139 灰底白卡 + 图标回名称格):** 高级列表,**分区容器 = 白 tile**(bg-card + 1px 描边 + radius-lg + space-2 内 padding,灰骨架底上的区域白卡,GH #139;末行去 hairline,容器边收口),列 = 模型(第一层级 md/600 墨色,中间截断 tailKeep=12 + el-tooltip 快显 200ms 全显;已停用行弱化 + 「已停用」xs 注;**名称格内联供应商瓦片**——26×26 统一品牌瓦片在模型 ID 左侧,见「供应商图标」条,GH #139 自供应商列移回)/ 协议(**协议原值纯文本**,sm secondary 截断 + title 兜底,130px 全显 images_generation——**2026-08-02 用户裁决:供应商文本列改协议列**(endpoint = 模型 × 协议,W3 粒度在列位可见;GH #139 恢复 family 文本列的处置被本轮取代;供应商身份由名称格瓦片独担 + tile title 兜底 family 全名;「一行不重复两枚图标」纪律随文本列退役自然消解);不经 protocolTagType 色槽 tag,列语言与供应商列一致)/ 状态(StatusBadge sm **裸词**——**2026-08-02 用户裁决:列表行不再挂「· 可用性 / · 延迟」成因副标签**,状态列 150 → 100 收窄;成因副标签归详情面板,列表层只读三态词)/ 24h 可用率(**数字左 + 24 格信号条右**:数字 md tabular-nums *-text 阶;**UptimeMicroStrip 复活**(2026-08-02 用户裁决,推翻 GH #131 退役)——24 格 = 过去 24 小时逐小时,格 tier 走 overviewDots 单源(ok/partial/fail/none=border 灰,图形阶),格 title = `dotTooltipText`(「HH 时段 · 成功 X/Y」/「无数据」),条 aria-label 汇总四档计数;连续 0–100 进度条退役,`availabilityBarWidth` 随删)/ P95 延迟(sm regular)/ 24h 趋势(TrendSparkline 行内 20px 道)/ 操作(**chevron-right 裸三角按钮** 18px,secondary、hover brand,aria-label「查看详情」——「详情」文字按钮退役)。**列头点击排序(GH #136):** 可排序列 = 模型/24h 可用率/P95 延迟,点列头切换正倒序、当前列带 ↑/↓ brand 指示(aria-label 载当前方向);新列首击方向 = 优在前(name 升 / rate 降 / p95 升),再击翻向;协议/状态/趋势/操作不可排序(无全序——类目/秩表/形态/按钮)。**默认 = 可用率降序(高在前,GH #136 用户裁决,推翻 GH #131「最弱领先首屏」默认;升序一击可达、复现旧口径)**;桶序不随键向变化:有值启用行(键向仅作用于桶内)→ 无数据启用行(null 率/null P95 沉于有值行之下,GH #131 口径存续)→ 已停用沉底;可用率同值回退 severity 秩(方向无关),终局 tie-break 模型名→协议→endpoint_id 永不随向翻转。排序状态机/持久化/纯函数集中 `utils/modelList.ts`(nextListSort/sortListEntries/loadListSort/saveListSort/listSortNote),**持久化 localStorage 键 `hs:list-sort`**(hs:dark 同族;坏 JSON/未知键向回落默认,storage 抛错吞掉);**状态由 DashboardView 持有**(工具条副注须重述当前排序——副注动态化,listSortNote 输出「(按可用率降序)」等,GH #131 静态「(按可用率排序)」会在用户改序后失实,防作假「标签必须与数据一致」),组件保持纯展示(sort prop 进、点击事件出)。行 = 共享 grid 模板(表头与行同一模板,列 x 对齐是构造属性;GH #139 列宽级联;2026-08-02 协议列修订 + 状态列收窄 + 可用率列扩宽:协议 130(取代供应商 100——images_generation 全显;26px 瓦片 + 8px gap 在名称格)/ 状态 100(裸词不带成因,自 150 收窄)/ 可用率 210(数字 + 24 格信号条,自 150 扩)/ P95 100 / 操作 40 固定(小计 580),名称 1.8fr(**floor 140**——模型身份是第一层级,禁裁没)/ 趋势 1fr 弹性(**floor 0**——2026-08-01 第九轮挤压带加固:断点已上移 1024,1024–~1280px 带内固定列最小和(792px)仍可超内容道,趋势道为指定减震器先缩向 0,保 P95/可用率可读;残余溢出由分区 tile `overflow: hidden` 在容器边逐级裁剪,**禁涂出格压邻列**(原「模型 ID 与后面的列重叠」实机缺陷);名称格 `overflow: hidden` + 已停用注 wrap 折行,列头词 nowrap 裁剪),容器白 tile 边与 padding 计入,算术注释在组件内)+ 行间 hairline(行全态无 radius——2026-08-01 第六轮实机反馈:border-bottom 随 radius-lg 在底部两角上卷成 U 形描边,每行读作带框小卡、首行卷边臂撞列头线;第七轮裁决:hover 填充也不带圆角,列表纯矩形语言,与 Leaderboard brand-soft 行填充同族;hover 时自身 hairline 隐去让填充盒干净);**行 hover = `--hs-bg-hover` 填充(GH #140 用户裁决:上浮 2px + shadow-md 在灰底白 tile 内穿帮,GH #139 LOW-2 已知项随本条关闭;卡片类可点元素由 role/tabindex/焦点描边承担,不再依赖浮起阴影)****名称格协议小标(GH #140)已退役(2026-08-02 用户裁决):** 协议身份改由协议列承担,名称格 = 瓦片 + 模型 ID + 已停用注;tag 形态存续于 protocol.ts 其他消费方(详情页标题行/EndpointTable/StatusCard chips)。**模型 ID 点击复制(2026-08-02 用户裁决):** 名称格 ID 为嵌套按钮(行 role=button 内的独立交互,click/keydown 双 stop——Enter/Space 复制不触发行开面板),hover tooltip 全显不变,点击复制走 `utils/clipboard.ts copyText`(非安全上下文 textarea 降级——测试线纯 http),反馈走 ElMessage(成功「已复制模型 ID:xxx」/失败警示);hover 墨色转 brand 作复制启示,focus-visible 2px brand outline。行 role="button" + tabindex + Enter/Space + data-endpoint-id(焦点归还锚点)。**分组回归(GH #140,撤销 GH #131 分组选择器退役):** 工具条 select 四项(不分组默认/按厂商/按能力/按协议,枚举单源),section-header = 厂商组带 26×26 瓦片(与行同剪影,能力/协议组裸组名)+ 组名 lg/600 + meta 行「N 个端点 · 异常 N · 降级 N」(计数按模型去重,措辞经 statusLabel 组句禁字面量)+ **组级 24 格信号条 + 组分享按钮(2026-08-02 用户裁决)**——信号条 = 组内 enabled 端点的探测加权聚合(aggregateDots24h,禁按端点简单平均,与行条同件 UptimeMicroStrip,flex 1 + max-width 320 + margin-left auto 钉右);组分享按钮(EP Share 图标裸按钮,secondary/hover brand)开 StatusShareDialog,快照 = 该组 entries + 当前筛选条件 + `group: { dimension, key }`(ticket 59 字段复活,物料范围 chips 以分组 chip 领衔——**推翻 GH #131「组分享入口退役」**,组子集永不读作全局);**组间排序 = 组内最重 entry severity 秩(`groupRank`,全禁用组沉底),组内排序 = 当前列头排序态**(sortListEntries,桶序规则不变;副注描述组内排序,与分组并存);分组是纯视图组织,筛选/持久化/分享快照零变化。**列表视图 URL 深链(2026-08-02 用户裁决):** 关键词 `q` / 供应商 `family` / 状态 `status`(显示态词)/ 分组 `group` / 排序 `sort=key:dir` 五参数经 `router.replace` 镜像(200ms 防抖,默认值不入参保持净 URL;query 变化不重挂载——App.vue transition key 在 path);打开时 URL 优先(排序回落 localStorage `hs:list-sort`,其余回落默认),编解码纯函数 `listSortToQuery`/`parseListSortQuery`(modelList,vitest 守卫)——关闭 brief 未决「筛选 URL 深链」。**瓦片渲染单组件(2026-08-01 窄屏批):** 组头/桌面行/窄屏卡三处 26×26 瓦片统一由 `VendorTile.vue` 渲染(自 ModelStatusList 抽取,映射仍单源 `utils/vendorIcon.ts`)。**窄屏卡片形态(2026-08-01 外壳抽屉批;2026-08-02 随协议列同步):** <1024px 列表形态切换为卡片流(Leaderboard 卡片先例;形态切换非横滚豁免)——卡首行 = 瓦片 + 模型名(同桌面截断/tooltip/点击复制)+ chevron,中行 = StatusBadge **裸词**(成因副标签与桌面同轮退役)+ 协议词(xs secondary;随桌面协议列同步,family 词退役——vendor 身份由首行瓦片承担)+ 已停用注,尾行 = 可用率数字 + **24 格信号条**(UptimeMicroStrip,与桌面同件)/ P95;**24h 趋势 sparkline 窄屏省略**(登记);列头行不渲染(排序为桌面交互,持久化排序态仍作用于卡片序);卡片几何与桌面行同一矩形语言(直角 hairline、hover 填充、自身线隐去)。
- **UptimeMicroStrip 是行内 24h 微条唯一组件(GH #115 首创;GH #131 退役,2026-08-02 用户裁决复活):** 24 格填满式(格高 14px、flex 槽、2px 间距、radius-xs),格 = 过去 24 小时的一小时;四档着色(ok `--hs-success` / partial `--hs-warning` / fail `--hs-danger` / none `--hs-border` 无数据灰,图形阶),tier 与格 tooltip 措辞同源 `utils/overviewDots.ts`(dotTier / dotTooltipText,与物料分段条构造性一致);**每格 el-tooltip 快显(show-after 200ms,GH #86 纪律——原生 title 约 1s 系统延迟不可控)**;条 `role="img"` + aria-label 汇总四档小时计数。**渲染位 = ModelStatusList 可用率列数字右侧 + 分组模式组头(组级聚合,见下)**(桌面行与窄屏卡 stats 行;取代连续 0–100 进度条);纯展示不可点,下钻走整行点击。
- **ModelDetailPanel 是模型详情面板的唯一组件(GH #116,取代 EndpointQuickViewDialog 与整行深链):** 自造右缘面板(teleport + scrim `--hs-overlay-bg` + 全高 sheet,**不用 el-dialog**——签名面自造,决策 8),五区自上而下:头部(模型名 h2 + StatusBadge md + causes + 已停用注 + status_reason)/ 三指标格(24h 可用率 tier 着色、平均延迟 P50 墨色 + P95 副注、错误率 tier 着色——全部快照推导 `utils/modelDetailPanel.ts`,null 显 `-` 不发明)/ 「24h 延迟趋势」(ProbeLatencyChart,180px)/ 「事件记录」(24h 失败事件:时间 + 流式/非流式 + 原因,空态「24h 内无失败记录」)/ 底部「打开完整详情」主按钮(→ /endpoints/:id)。**快照冻结:** entry prop 是行点击时冻结的快照,overview 10s 轮询不刷新已打开面板;异步区(延迟图 + 事件共享一次 24h 拉取)打开时一次性拉取,三态齐备,失败只污染该区。实时数据走完整详情页。**自造模态面三件套首例:** focus trap(utils/focusTrap.ts)+ ESC/scrim/按钮统一关闭 + 焦点归还触发行(父级承担)——规范条目见 DESIGN.md Signature 节。**不放分享入口**(分享弹窗只从筛选行与完整详情页开,弹窗不叠面板)。**宽度与头部构造(2026-08-01 实机修复):** sheet 宽 `min(440px, 94vw)` 且 `box-sizing: border-box`(content-box 下 94vw+padding+描边超宽顶出左缘);窄屏 padding space-4;panel-heading `flex: 1 1 auto` 钉住关闭按钮右缘;关闭按钮 `:focus-visible` = 2px brand outline(焦点单语言,打开即聚焦不落 UA 黑环)。
- **故障记录时间线(/alerts,GH #117;公开化与重构 spec 0019,2026-08-02 五项裁决):** AlertsView = h1「故障记录」+ 筛选条(模型/类型/时间范围:今天/24h/7d/30d,全部客户端过滤既有 limit 窗口,不发明第二服务端过滤口径)+ 时间线面板(轻容器)。**公开边界(附录第 16 项,W6 级):** 本页对匿名公开,但匿名读者只见**故障叙事四类**——down / recovered / group_down / group_recovered;test / batch / quiet_summary / score_drop / score_drop_skipped / retire_pending / retired 七类运维管道事件登录可见;分叉在服务端 API 层(匿名 payload 构造性不含七类,不靠前端隐藏),匿名视图全局(与公开状态板 overview 匿名全局同口径);匿名面类型筛选器只列四类(列出永不可能命中的七类 = 不诚实 UI)。**时间线三级嵌套(裁决 3):** 月 > 周 > 日;月/周可折叠(折叠是视图组织,事件不丢);轨道线跨日跨周连续不断,只在月组边界与面板顶/底收口;跨年时月标签带年(如「2025 年 12 月」),不另立年层级;日标签「今天/昨天」锚点沿置。**事件行构成:** 时间 + 轨道节点(tag type 着色)+ 类别 tag + 影响对象(模型名或 group_key;已删除端点回退裸 id 标签,审计面不丢行)+ 持续时间(FIFO 配对:`pairIncidentDurations` 把每条 down/group_down 与同 scope 内**其后第一条** recovered/group_recovered 配对,未配对者标「进行中」)+ 投递状态 + message。**行内展开详情(裁决 4,EvalLiveFeed 先例,否掉弹窗/面板):** 点击事件行就地展开——完整 message(不截断)、完整时间戳、事件 ID / 端点 ID、配对恢复事件与持续时长(复用 `pairIncidentDurations` 同一配对结果,经 paired 变体的 `closerId` 回查恢复事件,禁第二配对口径;配对行对 opener 未配对显「进行中」、对 closer 与非事故事件显中性「—」——无配对信息不冒充事故语义,check #144 LOW-2 登记)、投递状态明细(投递词双端单源 `alertSentLabel`——alertTimeline.ts 导出,行单元格与展开区共用,详情附 sent_ok 原始态,check #144 LOW-1 登记);端点事件附「查看端点详情」链接 → /endpoints/:id。**筛选 URL 深链(裁决 5,ModelStatusList 五参数先例):** model / kind / range 三参数经 `router.replace` 镜像(200ms 防抖、默认值不入参保持净 URL、打开时 URL 优先、编解码纯函数 + vitest 守卫)。分页走既有 `limit` 参数(50 → 100 → … → 200 服务端帽),帽到提示缩小范围。词表见 §7(十一 kind)。**AlertHistory 旧表格组件已删除**,告警历史唯一呈现 = 本页。构成规格见 alerts surface brief。
- **RecentEvents 是状态概览「近期事件」区的唯一组件(GH #132;卡片按参照稿重构 GH #138):** 最新告警事件横排四卡(桌面四列不折行)+「查看全部事件」→ /alerts。**登录态边界(main 裁决,2026-08-02 修订):** /alerts 页与 alerts API 的四类故障叙事已对匿名开放(附录第 16 项),但本区**不同步开放**——`authed=false` 时整区不渲染且零请求(状态概览首页的事件卡片是运营视角摘要,保持登录面;匿名读者的故障叙事入口是侧栏「故障记录」页);刷新复用 overview 轮询 tick,不另立定时器。**卡片构成(参照稿):** 顶行 = 时间(xs placeholder,HH:mm)左 + chip 右;标题(md/600 primary,两行截断,title 全显);meta 两行(xs secondary:影响范围 / 持续时长,时长仅 opener 卡有);**右下角水印图标**(≈44px,opacity 0.18,着色 = kind tag type 图形档,aria-hidden 纯装饰——chip 已承担类别词)。**chip 双轨口径:色 = 事件 kind 的 tag type(alertKindTagType,十一 kind 全覆盖;soft 浅底 + *-text 阶),词 = 状态或类型**(opener = 进行中/已恢复,point-in-time = alertKindLabel 类别词;每卡恒渲染 chip)——口径全文与参照稿出入登记见附录第 15 项。派生集中 `utils/recentEvents.ts`(eventChip/impactText/incidentDurationText/selectRecentEvents),组件只渲染;卡高 150px 锚定与骨架同形(算术注释在组件内);空态「近期无告警事件」永不读作「从无事故」。
- **Leaderboard 是评估榜单的唯一排行展示组件(/eval、/benchmark、/report/{token}、CampaignReportView 复用;v2 轻容器化 GH #118):** 每模型一行——`名次 │ 模型 │ 总分 │ 涨跌 │ 各维度列`,全表 CSS grid 定宽(名次 32 / 模型 260 / 总分 96 / 涨跌 80,维度列 `minmax(0,1fr)` 等分剩余宽,**列宽与权重无关**),列头与行共用同一 grid 模板,**每列 x 位置全表恒定**;模型名截断 + `title` hover 全显。**排序走点列头**(总分与维度列头可点,降序唯一方向,当前列带 ↓ 指示;再点当前列回总分降序——禁前端 reverse 制造第二排序口径;列头 role/tabindex/Enter/Space 键盘可达);family 过滤走榜单上方工具条;行下钻走 `el-dialog`(EvalRunDetailDialog 模式,公开页 `selectable=false` 行不可点);工具条收敛为 family 筛选 + baselineNote + 分享按钮;**涨跌列常显**(贴着总分;行级口径:▲success-text ▼danger-text,持平/不可比/无基准 `–` 占位)。**表格化节奏(ticket 82 沿置):** 列头下 1px `--hs-border` hairline + 行间 1px `--hs-border-light` hairline + 行高恒 46px;行 hover/focus 填满行盒。**前 3 名仪式:** 行左缘 3px `--hs-brand` 竖条(v2 蓝品牌;非前 3 名行保留透明竖条位,列 x 恒定)+ 名次数字 `--hs-text-lg`/600 brand,其余 secondary;live 模式(rank `–`)不渲染竖条。**v2 视觉变更:** 轻容器外壳(白面 + 1px 描边 + radius-lg、桌面 padding 24/32 窄屏 16)、文字场景 *-text 阶、**矩阵行 hover 上浮豁免**(GH #118 裁决:行可点性由 hover 填充 `--hs-brand-soft` + 指针承担——hairline 行上浮破坏共享 grid 基线与仪式竖条几何)。窄屏降级卡片式列表(share-materials brief 规格,GH #94;断点 2026-08-01 自 768 上移 1024)。**/benchmark 页 h1 =「Benchmark」**(GH #118 裁决:与侧边栏 IA 词对齐;「评估榜单」保留为描述性短语/lede)。
- **Leaderboard 运行中半成品模式(ticket 52 / GH #40 沿置):** 未完成批次下榜单可查看但——① 半成品总分非 null 的行显示**实时名次**(行序位次数字,弱化样式:sm + secondary,**禁前 3 名竖条/大字/徽章**);半成品总分 null 的行名次列 `–` 占位(placeholder);② 行序按**半成品总分降序**(ADR-0005 既有口径:已判维度加权平均,未判维度不进分子分母——禁止「含 null 维度按 0 计」的第二口径),同分 model_id 字典序,null 总分沉底;**列头仍禁点**(实时榜只有总分降序一个口径),保留 family 过滤;轮询行重排不做动画;③ 涨跌箭头列整列隐藏(不占位);④ 已判分维度格子照常;未判分格子 value 槽显**批次状态词**「进行中/等待中/失败」(xs,按 §3.2 批次/运行状态色映射 *-text 阶;**纯文字禁圆点禁闪烁**)+ 空轨道,tooltip「能力点名 · 等待中/进行中/失败」;**run 完成但全部判分失败、score 仍为 null 的格子显「未判分」**(中性 placeholder,非第五状态词);⑤ 总分列墨色数字 + **空轨道不染档色、不填充**——半成品总分构造上不可能被档色读作「差」;行尾灰字注「N 个维度进行中」,有失败 run 追加「· N 个失败」(danger-text);⑥ settle 后名次弱化样式升级为正式名次样式,不做强调动画,转场提示由 ElMessage 承载。
- **Leaderboard 判分不完整模式(ticket 92,spec 0014 决策 A 沿置;与半成品模式互斥——live 行无 `complete` 键,settle 行无 live-note):** settle 批次 `complete=false` 的行——① 名次 `–` 占位;rank-top 竖条与前三大字按 **`row.complete !== false && index < 3`** 判定(禁纯 index);② 总分 `–` + 空轨道不染档色不填充(无总分即无档色);③ 涨跌 `–` 占位,tooltip「**判分不完整,不参与排名与涨跌**」;④ 维度格子照常渲染真实分数(不掩盖已判分事实);⑤ **水印挂模型列、模型名之下第二行:**「**判分不完整,缺 N/M 维度**」——「缺」字必须保留(裸 N/M 与 ScoreCell 覆盖率水印「·8/10」分子口径冲突);N = `missing_suites` 契约字段,M = missing_suites + 该行非 null suite_scores 数(行级自洽推导,恒 N ≤ M);规格 xs + secondary + font-weight 400 + opacity 0.85,单行优先、物料窄列允许自然换行,**禁截断 + title 兜底**(物料无 hover);⑥ 两模式区分由构造性差异承担(live = 半成品墨色数字 + 动态注,settle 不完整 = dash + 静态水印;涨跌列隐藏 vs 常显;永不同时出现);⑦ **排序:`complete=false` 恒沉底**(与排序列无关),不完整组内 model_id 字典序;/benchmark `sortRows` 镜像同一口径(`complete === false` 视为不完整,`undefined` 视为完整),禁第二排序口径;⑧ 全部模型不完整时榜单照常渲染(行保留、全体 dash),禁空态文案冒充「暂无上榜模型」;live 模式呈现零变化;⑨ **EvalCard 物料同步:** `rank: number | null`(不完整行名次 `–`;rank-top-rail 按 `rank !== null && rank <= 3`)+ 模型名下同款水印 + 总分 `–` 空轨道 + 涨跌 `–`;⑩ 暗色:占位与水印消费 placeholder/secondary 语义令牌(暗色后置期随亮值,暗色 spec 填值)。
- **ScoreCell 是榜单维度格子的唯一共享组件(ticket 78 沿置;Leaderboard 行与 EvalCard 静态模式双端消费):** `formatScore` 数字(md/600,**档色着色 *-text 阶**)+ 其下 4px 档色细条(轨道 `--hs-bg-hover`,radius-xs)。语义:**轨道中性化**(bg-hover,不用 border-light);**条刻度恒 0–100**(W7 绝对分制的视觉镜像,87.5 的条永远比 100 短一截,跨行跨批次可比);档色按 §3 分数档位(≥80 success / ≥50 warning / <50 danger,阈值以后端口径为准);null(未判分)→ `–` 占位 + 空轨道。**覆盖率水印(防作假硬约束):** done 且 `judged < expected` 的格子分数后随压缩水印「·8/10」(同色弱化 400 + 0.85),**宽度 ≥80px 才渲染**,不够则省略,完整置信信息由 hover tooltip 恒兜底「能力点名 · 分数 · 判分 X/Y 题 · 采样 N 次 · 耗时 X · Token Y」;未判分格子 tooltip 为「能力点名 · 等待中/进行中/失败」。静态物料(EvalCard)无 hover:水印按同一宽度规则渲染(staticMode 只测一次宽度),tooltip 信息不进物料(登记信息差)。props 接缝 `{ name, score, cell, staticMode? }`。总分列是同族变体:`--hs-text-xl`/600 **墨色不染档色** + 6px 档色条(0–100 同刻度),层级靠字号与条粗。
- **EvalProgressGrid 是批次进度矩阵的唯一组件(沿置;v2 点词分离 GH #120):** 模型 × 能力点状态矩阵,运行中/等待中批次的默认视图;首列模型名截断 + `title`,能力点列 flex 等宽,行随模型数纵向滚动,**不开横向滚动豁免**;单元格四态 = **状态圆点(基色)+ 状态词(*-text 阶)分离着色**(§3.2 批次/运行状态色映射);批次级进度汇总置于网格上方同卡片内;「进度网格 / 实时分数」视图切换走卡片顶部 el-radio-group。**分享页只读模式(ticket 54):** /report/{token} 复用本组件但不渲染视图切换。**批次成本汇总(GH #42):** card-top 批次进度行追加「判分耗时 X · 批次用时 Y · Token Z(输入 I / 输出 O)」——判分耗时 = Σ latency_ms,批次用时 = wall-clock(finished_at − started_at,running 取当前时刻;started_at 缺失显 `-`),两者并列各司其职,**禁互相冒充**;Token 输入/输出分列(sm + secondary;token null 的 run 按 0 计入求和但明细显 `-`);**只读模式(分享面)不渲染成本汇总与成本段**(成本是运营数据,超 ticket 54 公开范围)。窄屏:模型列 96px、cell 词省略留点(GH #94)。
- **EvalLiveFeed 是批次判分动态流的唯一组件(issue #17 / GH #41 沿置):** 运行中批次题目级判分事件流,行内六要素(时间/模型/评估集/题目/判定方式/得分+耗时),最新在前;**行内展开详情**(否掉弹窗——feed 是流式监控面板):展开区四块纵排(题目全文 / 期望答案——label 随判定方式分叉:rule=「期望答案」、judge=「评分要点」/ 模型作答 / 裁判结果),按需拉取 + 按 id 缓存,展开态 keyed by id,轮询新条目前插不塌;判定方式 tag 用 `el-tag type="info"` 中性灰(**类别属性非健康信号**);得分 `formatScore` + 档色 *-text 阶,**null 分(裁判失败)渲染 `-` + hover「裁判失败,未判分」,永不读作 0 分**(W7 视觉镜像);增量合并与封顶(LIVE_FEED_CAP=200)走 `utils/liveFeed.ts` 纯函数;组件自身零 setInterval(游标与轮询由父级视图持有)。**边界:控制台专属**——期望答案/rubric 是题库内容(W7),永不出分享面与公开页;settle 后卸载,历史批次明细走 EvalRunDetailDialog。
- **评估成本指标(耗时/Token)呈现约定(GH #42 沿置):** 耗时与 Token 是**成本指标,不是质量指标**——一律中性色(secondary),**禁止与档色/状态色挂钩、禁止闪烁、前端不自定「快/省 = 好」分界线**。格式集中(utils/format.ts):题级 `formatMs`、批次累计 `formatDuration`、Token `formatTokens`(k/M 一位小数),input/output 分列标注。位置两层:批次汇总 = EvalProgressGrid card-top;明细 = 进度网格 cell tooltip + ScoreCell tooltip + 报告页「运行成本明细」表(**仅 settle 批次渲染**——运行中由汇总 + 双 tooltip 承载)。**榜单矩阵本体不加成本列**(列 x 恒定不变式 + 防质量暗示)。**边界:控制台专属**——分享面与静态物料不渲染任何成本指标。
- **分享报告页运行中信息边界(ticket 54 沿置,防作假核心):** 分享面(/report/{token})未完成批次只公开**运行状态与判分覆盖**(模型 × 能力点四态 + X/Y 题),**不公开任何分数、名次、涨跌**——「实时分数」为登录控制台专属,分享页不提供切换入口;批次 settle 后分享面照常渲染完整榜单。依据:状态/覆盖率是运行元数据而非评估结论;已知并接受的增量:运行中分享面可见 per-model 失败归因(失败是运行事实,不掩盖;瞬时、token 可控、走审计)。分享页不新增任何依赖会话接口的交互入口;「保存图片」纯客户端生成(props 快照 + snapdom + 本地下载/剪贴板),ADR 0006 不变式守住。
- **TrendChart 是趋势类折线图的唯一通用组件(沿置 + GH #114 轻量化):** 裸图表(不带卡片,布局由父级负责),默认在 null 点断线(未判分批次不得连成假分),支持竖向断点标注线(占位灰虚线,如「v2 起题目变更」);色板走 `utils/chartColors.ts` 单一来源;平滑走 `utils/monotoneSmooth.ts` 单调插值(§3.3;实测点保留 5px 极简点)。
- **ModelTrendDialog 是报告页行下钻的唯一趋势弹窗(沿置):** 按模型按需拉取 `/api/campaigns/{id}/trends`,分数线(版本断点标注「vN 起题目变更」+ 判分口径断点标注「判分口径已变更」,同一位置双断点合并为一行标注,复用 TrendChart 灰虚线断点机制)+ 探测成功率/延迟并列;已删除模型带「已删除」tag;加载/空/错误三态齐全。
- **EvalShareDialog 与 StatusShareDialog(分享弹窗,v2 沿置 + 物料重建 GH #121):** 结构机制不变——预览(限高 62vh 滚动)+ 离屏双份捕获(`position: absolute; left: -10000px`,snapdom 祖先 overflow 裁剪规避;**禁 `position: fixed`**——snapdom 按视口宽度重排舞台)+ 复制图片/下载 PNG + 复制降级(非安全上下文置灰 + 提示,下载永可用)+ **恒亮主题捕获**(暗色后置期天然满足;暗色 spec 落地时恢复 `withLightCapture` 级联)+ 失败不锁按钮。入口:状态概览筛选行「分享状态」主按钮(全局快照 = 筛选后 entries,打开即冻结)+ EndpointDetailView 单模型分享 + Leaderboard 工具条「分享图片」(控制台)/「保存图片」(公开分享页,`shared` 口径;仅 settle 批次渲染——半成品分数不外流)。**版式切换(沿置):** StatusShareDialog 预览上方「完整版/紧凑版」radio,默认档 = 打开时视口 <1024 一次性判定(断点 2026-08-01 自 768 上移);文件名紧凑版加 `-compact`。报告页「复制链接」(铸 token,ADR 0006)与图片分享并存消歧。
- **StatusCard / EvalCard 物料(v2 新视觉重建,GH #121):** 构成与五形态规格见 share-materials surface brief;语义约定全部保留——外框三段(品牌区/范围行 chips/页脚)、**结论必须标注统计范围**(批 56:无筛选「全部端点」,有筛选逐项 chips 一个不漏,零匹配中性「暂无数据」永不读作「全部正常/全部稳定」)、数字与 chips 同源同快照、防作假水印与断点口径、空态不冒充。视觉变更 = 蓝品牌区(BrandMark 蓝渐变 + 品牌条)、三态词表、hero 72 档大数字(完整版)/ 3xl 锚点(480 族)、hairline 卡内分隔、文字场景 *-text 阶、小卡 av-* 类补登(GH #93 潜伏 bug 修复)。
- **角色 tag(v2 色板重登记,GH #119):** 词表四词不变(超级管理员/管理员/操作员/观察者);映射不变——super_admin/admin → `primary`(解析为品牌蓝 #007AFF)、operator/viewer → `info`(解析为 `--hs-info` 中性灰);语义 = 权限层级非健康度,禁借用状态色。**消费方变更:** AppHeader 已退役;AppTopbar 用户 chip 与 AppSidebar 用户卡均以**纯文本 roleLabel** 呈现(未用 el-tag,secondary 中性——外壳高密度区无 tag 容器,登记为新消费方口径);UserManager(系统设置页)保持 el-tag。实现集中 `utils/role.ts` 不变。
- **协议 tag(v2 色板重登记,GH #119):** 词表 = 协议原值(anthropic/openai/images_generation/images_edit);映射不变——anthropic → `success`(解析为 #34C759)、openai → `warning`(解析为 #FF9500)、images_* → `info`;语义 = 契约家族区分非健康度(GH #34 裁决沿置:存量 chat 配色与状态色的张力登记为已知并接受)。实现集中 `utils/protocol.ts` 不变。**消费方变更:** EndpointCard 组内同值收敛例外随卡片退役;tag 形态现存消费方 = EndpointDetailView 标题行、EndpointTable(模型管理)、StatusCard 范围 chips 与端点小卡模型行;**ModelStatusList 协议列(2026-08-02)= 协议原值纯文本,不经本条色槽 tag**(列表域列语言为纯文本;GH #140 名称格色槽小标同日退役);按协议分组时组名即协议。
- **供应商图标(vendorIcon,参照稿复刻批第二部分,2026-08-01 登记;统一瓦片化 + Kimi 增补 GH #136;渲染位 GH #139):** ModelStatusList **名称格(模型 ID 左侧)**渲染**统一 26×26 品牌瓦片**(GH #139 自供应商列移回名称格——取代 GH #136「供应商列图标化、family 文本退役」的处置;GH #139 恢复的 family 文本列 2026-08-02 改协议列,一行不重复两枚图标纪律以语义位交换形态存续)——已知供应商 = 品牌色实底 + 白色图形(anthropic 珊瑚 #D97757 / googlegemini 参照蓝 #4E86F5 / openai 黑 #000 / bytedance 官方蓝 #3C8CFF / alibabacloud 官方橙 #FF6A00 / **moonshot(Kimi,GH #136 新增)** 黑 #000 底 + 双路径标:品牌蓝点 #1783FF + 白 K);瓦片内图形 16px,半径 radius-sm;未知供应商回落 GH #131 首字母瓦片(familyInitials,bg-hover 中性底 + secondary 字)。**统一瓦片取代 GH #134「命中后去中性底只留图形」形态**——全供应商同一剪影(高宽一致、有背景),近黑图形暗色不可见问题随实底自然消解,**openai 的 currentColor 例外随之退役**(改黑底白图形,登记口径更新)。**映射单源 `utils/vendorIcon.ts`**(role.ts 集中原则):family 归一(大小写/别名——分类器 canonical 值 claude/gpt/gemini/qwen/doubao 与厂商名 anthropic/openai/google/alibaba/bytedance 双覆盖;kimi/moonshot/moonshotai 归一 moonshot)→ **{ tile, paths: [{ d, fill }] }** 或 null(数据结构自 { path, color } 重构:多色标需要分路径 fill,kimi 是当前唯一多色标),组件禁自带映射。**图形标识豁免(BrandMark §2b 同类):** 供应商品牌色(#D97757/#4E86F5/#3C8CFF/#FF6A00/#1783FF)与瓦片中性字色(#fff 图形/#000 底)是外部品牌资产、非语义表达,豁免调色板纪律,色值内联映射表并注释登记;check-tokens 豁免表(整文件)与 .impeccable/config.json(design-system-color 品牌色 scoped,GH #136 增 #1783FF)已同步登记。**hex 偏差纪律:** 与 simple-icons 官方 hex 不一致的取值必须登记偏差(官方值→采用值+理由)——anthropic 官方 #191919 → 采用 #D97757(Anthropic 官网真实品牌珊瑚色,贴参照稿);googlegemini 官方 #8E75B2 → 采用 #4E86F5(参照稿蓝);kimi 路径取自官方标(非 simple-icons),蓝点 #1783FF 为官方品牌蓝。**例外取舍(登记):** bytedance 取官方蓝 #3C8CFF 弃参照稿红圆(品牌资产优先于参照稿占位色)。**三变体与热门厂商包(GH #140):** 瓦片变体三分——brand(品牌底白图形:既有六家 + deepseek #4D6BFE + grok 黑底)/ subtle(浅底原色:glm 渐变 #504AF4→#3485FF、mistral 五行色块、minimax 渐变 #E2167E→#FE603C、wenxin 渐变 #0A51C3→#23A4FB、meta 供 llama 用)/ none(免瓦片圆标:hunyuan 整圆自带 #0055E9 底);别名扩(deepseek/glm=zhipu·chatglm/mistral/grok=xai/hunyuan=tencent/minimax/wenxin=ernie·baidu/meta=llama);渐变 id 全包唯一(vitest 断言);多色标来源 lobe-icons 官方路径。**范围:** 仅列表名称格(参照稿范围;GH #139 自供应商列移入);物料/详情面板复用同款图标登记为后续可选。图标 svg `role="img"` + aria-label = family 名;瓦片容器 overflow hidden + title 兜底 family 全名(family 文本列 2026-08-02 改协议列后,tile title 为 family 全名唯一兜底)。
- **反馈三件套(不变):** 操作结果 → `ElMessage`;破坏性操作 → `ElMessageBox.confirm` 二次确认;表单校验失败 → 表单内联提示。
- 新组件若为通用展示单元(非业务组合),放 `components/` 并在本文件登记;业务一次性组件就近放视图内。

## 6. 交互规范

- **三态必备(不变):** 加载态(skeleton 或 loading)、空态(空数据说明 + 引导操作)、错误态(错误原因 + 重试入口),任何数据区块缺一不可。**失败不冒充空态**(EndpointDetailView 评估区先例:加载失败 ≠ 暂无数据)。
- **长文本(不变 + v2 具名):** 模型名/Hub 名/错误信息一律截断 + hover 全显;模型名走中间截断(`utils/truncate.ts` splitMiddle,tailKeep=12,尾段保区分度后缀);全显走 `el-tooltip` 快显(show-after 200ms,GH #86 裁决——原生 title 约 1s 系统延迟且样式不可控;**常滚与 hover 跑马灯均否决**——「有东西在动」读作「有情况」,且慢于即时全显)。
- **轮询(不变):** `setInterval` 必配对清理;一切轮询走 `utils/visibilityPoll.ts` 的 `createVisibilityPoll`(状态概览 overview 10s、隐藏降频 60s、回前台立即刷新;批次类 3s/1.5s 隐藏暂停),禁第三套轮询实现;卸载调 `handle.clear()`。
- **批次 settle 转场轮询口径(ticket 52,不变):** 观察到 done/failed 的那次轮询响应即停轮询,渲染以该次数据为准;完成提示走 ElMessage(带批次号与原因);/eval、报告页、/report/{token} 三处同口径。
- **即时反馈(不变):** 点击类操作请求期间给 loading 或禁用态;可点汇总项有反馈态且可再点取消。
- **榜单/报告类消费页三态与轮询(不变):** 批次切换器空态、榜单空态、下钻趋势加载态;未完成批次呈现进度态不显示半成品名次;仅选中未完成批次时轮询。
- **导出物料的复制降级(批 56,不变):** 「复制图片」非安全上下文置灰 + 降级提示;下载永可用。
- **自适应展开区与披露容器(ticket 89 + GH #52 双轨,不变):** 条件触发展开区禁默认态占位、平滑高度过渡(grid `0fr→1fr` + 内层 `min-height:0/overflow:hidden` + `visibility` 延迟切换)、压扁振幅;**仅用户点击触发过渡;数据/筛选驱动的折叠与恢复走 no-motion 瞬切**;reduced-motion 由全局归零覆盖。
- **动效编舞的 JS 阶段门控(不变):** 多阶段动效延迟不受 CSS 归零兜底,必须 matchMedia 门控;常量在编舞实现处定义并注释互指;编舞逻辑抽纯函数(vitest 覆盖 reduced-motion 双分支)。当前在编:数字补间(numberTween)与图表入场(chartMotion)均按此落地。
- **位图物料渲染纪律(ticket 89,不变):** 后端位图(图形码 PNG 等)不走语义令牌、不做暗色适配、禁 CSS 滤镜;固定尺寸容器 + 1px border 描边 + radius-sm;加载/失败态同尺寸防位移,失败给原因 + 重试。

## 7. 文案规范

- 界面一律简体中文;**词表分四套,互不混用:**
  - **域模型 endpoint 状态(后端域,界面不直接显示):** 正常 / 降级 / 宕机 / 告警(W5;出现于 API、告警管线、代码注释)。
  - **显示层三态(界面唯一状态语言):** 稳定 / 降级 / 异常(§3.1,唯一映射 utils/statusDisplay.ts,不新增同义词;词表 2026-08-01 自「稳定/性能下降/服务异常」切换,GH #128)。
  - **批次/运行状态:** 等待中 / 运行中 / 已完成 / 失败(campaignStatusLabel 既有口径)。
  - **告警事件类别:** 见下条。
- **告警事件词表(十一 kind,GH #98 九词扩十一词;审计/历史域的类别词表,与状态词表并列、互不混用):** 中文标签集中 `web/src/utils/alertKind.ts`(集中原则先例,组件内禁写词字面量)——down「故障」/ recovered「恢复」/ score_drop「分数大跌」/ score_drop_skipped「对比跳过」/ test「测试」/ group_down「厂商组告警」/ group_recovered「厂商组恢复」/ batch「聚合发送」/ quiet_summary「静默摘要」/ **retire_pending「待退役」/ retired「已退役」**(GH #98,spec 0018·端点退役 T4——该 spec 与 UI v2 的 spec 0018 编号撞车,见附录第 12 项);未知 kind 回落「未知类型」。**语义边界:本词表描述「这条事件是什么」,不承载健康状态语义,§3 语义色映射不动。** **公开分层(附录第 16 项):** 十一 kind 分两层——故障叙事四类(down/recovered/group_down/group_recovered)对匿名公开;运维管道七类(test/batch/quiet_summary/score_drop/score_drop_skipped/retire_pending/retired)登录可见,分叉在服务端 API 层。 tag type 映射:down→danger、recovered→success、score_drop/score_drop_skipped→warning、test→info、group_down→danger、group_recovered→success、batch/quiet_summary→info(投递形态记录,非健康信号)、**retire_pending→warning(需要注意)、retired→info(投递记录,非健康信号)**;未知 kind 回落 info。**借字例外(spec 0017 裁决沿置):** 「厂商组告警」的「告警」借自域模型状态词表,是「互不混用」纪律下的唯一具名例外——此处「告警」是事件类别词,语境限定故障记录页,与显示层状态域不相遇;`utils/alertKind.ts` 注释与本条互指,禁「修正」为其他词。
- **降级成因副词表(ticket #7,spec 0013,不变):** **可用性**(24h 成功率低于 95%)/ **延迟**(24h P95 超过 2× 7 天 P50 基线);仅作「降级」的成因修饰出现(§3.1 副标签),**不是第四显示状态词**;批次/运行域不借用;评分域既有文案不受管辖。
- **分数展示统一 0–100 整数**(null → `-`),`formatScore` 集中于 `utils/format.ts`,组件内禁止自写 `toFixed` 分数格式;0~1 原始分只存在于 API 层。
- 按钮用动词短语(「触发同步」「新建 Hub」);错误消息必须带原因,不只说「失败」。
- 数字与时间格式统一走 `utils/format.ts`;评估成本格式同此集中(GH #42):题级耗时 `formatMs`、批次累计耗时 `formatDuration`、Token `formatTokens`(k/M 缩写一位小数),输入/输出分列标注;成本指标中性色不挂档色(§5 成本约定)。
- **中文标点分界(2026-08-01 修正,T13 误登记纠偏):** 对外中文物料(GitHub Release Notes/issue 正文/部署通告/ADR 等面向读者的说明)走 zh-punct skill 全角标点(fix/check/selftest + 线上回读);界面 UI 字符串按仓内存量惯例(当前半角为主,混用容忍,不做机械统一);代码注释英文。

## 8. 规范的维护

- 本文件(业务语义手册)由 `plan` agent 维护;设计评审中做出的新约定按层回写——视觉/布局 → DESIGN.md,页面/组件 → surface brief,业务语义 → 本文件(回写前置与批次节奏见 collaboration.md §3),不回写视为未约定。
- 本文件与 [load-bearing-walls.md](./load-bearing-walls.md) 的关系:本文件管「业务语义一致性」,承重墙管「系统语义」;冲突时以承重墙为准(状态机四态语义由 W5 决定,本文件只做显示层映射与词表约定)。
- 与 ProxyHub 上游规范的关系:**已解除**(ADR 0015)——v2 起 HubScope 视觉体系独立演化,不回写上游,不再跟踪上游刻度。

---

## 附录:v2 裁决登记(本批新立;旧附录 A/B 随旧世界作废,历史在 git)

1. **ADR 0015 三推翻(2026-07-31,spec 0018):** 品牌色 teal → #007AFF(白底 4.02:1 不过 AA,用户已知并接受;品牌色文字从简、关键文本墨色);四态词表 → 显示层三态(failing 并入服务异常;域模型四态与告警管线不动);闪烁独占 → 禁闪烁 + §15 动效体系。替代方案(定向演进/深色蓝保 AA/英文主显)均经用户否决,记录在 ADR。
2. **功能色图形档本体按简报原文执行:** #34C759 / #FF9500 白底 2.2:1 不过 3:1 图形门槛——处置 = 文字/图形分工(*-text 阶实测均过 AA 4.5),本体不改(改本体会推翻简报锚点);已知代价登记在 ADR 0015 与 tokens.css 注释。
3. **三态映射接口(GH #113 main 裁决;词表经 GH #128 更新,见第 14 项):** utils/statusDisplay.ts 唯一映射点,接口六件(statusDisplay/statusLabel/statusTone/toDisplayStatus/displayStatusCounts/DISPLAY_SEVERITY_ORDER);色槽抽象,文字 *-text 阶、图形本体阶;未知 →「未知」+ warning 槽。状态筛选跟显示词表走(incident 档同筛 down+failing);物料分布串四段→三段,failing 计数由「含 N 个告警」事件 chip 披露(防作假不回退)。
4. **图表平滑与逐探测例外(GH #114):** 单调插值唯一来源 utils/monotoneSmooth.ts(否掉 ECharts smooth);ProbeLatencyChart 不平滑(tooltip 保真红线 + 尖峰是事故证据);样式口径与动画门控见 §3.3。
5. **hover 上浮豁免表格行(GH #118 main 裁决):** §15 的 2–4px 上浮针对卡片类可点元素;hairline 分隔的矩阵行上浮破坏共享 grid 基线与仪式竖条几何,行可点性由 hover 填充 brand-soft + 指针承担。ModelStatusList 行(圆角盒卡片读感)与 Leaderboard 矩阵行(表格读感)分属两侧,差异登记。
6. **页面 h1 = 侧边栏标签(GH #118 main 裁决):** 消除「评估榜单」vs「Benchmark」类漂移;描述性短语退居 lede。深链页 h1 为对象名,不受管辖。
7. **「错误趋势」范围收窄(GH #116 main 裁决):** 详情面板错误维度由三处承担(延迟趋势图故障窗 markArea + 错误率指标格 + 事件记录),独立错误趋势图与 markArea 冗余,不另立;spec 0018 §10 story 34 按此口径执行。
8. **自造模态面三件套(GH #116 check MEDIUM-2 沉淀):** focus trap + ESC/scrim 统一关闭 + 焦点归还,为一切自造 aria-modal 弹层的基线;utils/focusTrap.ts 为唯一 trap 实现。
9. **告警事件词表九词扩十一词(GH #98):** retire_pending「待退役」(warning)/ retired「已退役」(info),集中 utils/alertKind.ts;借字例外与语义边界条款沿置不变。
10. **版权行恢复(GH #122,保守裁决):** PublicFooter 退役带走「© 2026 HubScope」,spec 0018 未载有意去除——恢复于侧边栏底部(AppSidebar side-footer)。
11. **:deep(.祖先类) 死选择器纪律(GH #121 check HIGH-1 沉淀):** 见 §4;跨组件变体只走「子组件 prop 绑自身类」或「父组件 .ancestor :deep(.child)」。
12. **编号撞车登记:** 仓内存在两个「spec 0018」——UI v2.0 完全重构(docs/specs/0018-ui-v2-apple-rebuild.md,GH #109)与生成式端点 Ping 监测与端点退役机制(GH #96 母票,无独立 spec 文件落档)。引用时须带限定词(「spec 0018·UI v2」/「spec 0018·端点退役」);后续 spec 编号从 0019 起,撞车不重排(历史引用已散见各票)。
13. **family 词表统一「厂商」(2026-08-01 main 裁决,GH #122):** Leaderboard 筛选 UI 与 EvalCard 范围 chip 的「系列」全部统一为「厂商」(与 GH #59 范围 chips 既定词表一致);评估域 family 数据字段名不动,仅 UI 文案。
14. **显示层三态词表切参照稿(2026-08-01 用户裁决,GH #128):** 显示层三词自「稳定 / 性能下降 / 服务异常」切换为参照设计稿词「**稳定运行 / 降级 / 异常**」——「降级」与域模型四态词的「降级」同字不同域(显示层 degraded 档 vs 域模型 degraded 态,恰好同义,消歧无负担);「异常」同时是事件域通用词(异常明细等),状态词与事件措辞靠语境分层,不改事件文案。唯一映射点 utils/statusDisplay.ts 一处改全站生效;域模型四态(W5)、告警事件词表(§7 十一 kind)、降级成因副词表(可用性/延迟)全部不动。spec 0018 与 ADR 0015 中的旧词为历史决策记录,不回改。**2026-08-02 用户裁决再修订:「稳定运行」简化为「稳定」**(两字,与降级/异常同格,列表密度考虑)——同样由 statusDisplay.ts 单源一处改全站生效,本条其余沿置。
15. **近期事件卡 chip 双轨口径(2026-08-01 main 派发裁决,GH #138):** 事件卡 chip 的**色 = 事件 kind 的 tag type**(alertKindTagType,十一 kind 全覆盖,组件禁自造映射;soft 浅底 + *-text 文字阶),**词 = 状态或类型**——opener 事件(down/group_down,有配对条目)词 = 进行中/已恢复(GH #132 配对口径不变);point-in-time 事件(含 recovered/group_recovered 自身)词 = 事件类型词(alertKindLabel:恢复/测试/聚合发送/待退役……)。每卡恒渲染 chip。**与参照稿的出入登记:** 参照稿「已恢复」chip 有橙/蓝/绿多色,不映射本仓数据模型——已恢复**事件**的 opener 卡 = danger 色「已恢复」(事件本体是 down),recovered **事件自身**的卡 = success 色「恢复」;双轨执行,不逐字复刻参照稿配色。取代 GH #132「进行中 danger / 已恢复 success 两色、非 opener 无 chip」口径。
16. **故障记录公开化边界(2026-08-02 用户裁决,spec 0019):** /alerts 页与 `GET /api/alerts` 对匿名开放,边界 = **故障叙事四类**(down / recovered / group_down / group_recovered;recovered/group_recovered 是 point-in-time 事件但属事故叙事的收尾,含在公开层);**运维管道七类**(test / batch / quiet_summary / score_drop / score_drop_skipped / retire_pending / retired)保持登录可见。**分叉在服务端 API 层**(匿名 payload 构造性不含七类,不靠前端隐藏;hub-less 事件同样不得进匿名 payload);匿名视图全局——与公开状态板 overview 匿名全局同一口径(多 Hub 部署下匿名读者本就看见全部 Hub 的模型与状态)。**RecentEvents 不同步开放**(裁决 2):状态概览「近期事件」四卡保持登录渲染,匿名入口是侧栏「故障记录」——本条是对 GH #132「alerts API 是会话保护面,公开状态板对匿名读者零泄露」登记的部分推翻(API 与 /alerts 页放开四类,首页事件卡不放开)。**附带裁决:** 时间线月>周>日三级嵌套可折叠 + 轨道跨日跨周连续(裁决 3);事件行内展开详情(裁决 4,EvalLiveFeed 先例);筛选 model/kind/range URL 深链(裁决 5);`alerts.go` 「Public (read)」陈旧注释随实现一并修正。**匿名 payload 字段口径:** DTO 字段不变(id/endpoint_id/kind/message/sent_ok/created_at/group_key)——message 内错误摘要与公开 probes API 的 error_summary 同级(已公开),sent_ok 投递状态对匿名可见(2026-08-02 用户落锤:不分叉全员可见——投递记录是事故证据链的一部分,非敏感);**group 事件 message 含 Hub 名分段是新增匿名暴露面——2026-08-02 用户落锤:接受并登记**(Hub 名只是租户标签,公开状态板本就全局暴露全部 Hub 的模型与状态,信息层级一致;alerter 文案与历史行零改动)。

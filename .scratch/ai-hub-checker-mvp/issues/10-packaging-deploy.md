# 10 — 打包与部署

**What to build:** 交付形态收尾:一条命令完成前端构建 + Go 编译,产出内嵌前端与迁移的单二进制;提供备选 Dockerfile;部署文档覆盖 systemd 常驻、nginx 反代内网域名、环境变量清单(监听端口、数据文件路径、管理员口令)与首次启动后的初始化顺序(登录 → 录 Hub → 确认自动发现)。文档同时写明凭证安全约定:token/口令不进 git,Hub token 界面脱敏。

**Blocked by:** 04 — Endpoint 详情页 + rollup 与数据清理;05 — 模型自动发现;06 — 飞书告警;07 — 管理员认证;09 — 评估中心 UI + 每周定时 + 分数大跌告警

**Status:** ready-for-agent

- [ ] 单条命令产出可直接运行的 Linux 单二进制(前端与迁移内嵌)
- [ ] 备选 Dockerfile 可构建可运行,数据文件经卷持久化
- [ ] 部署文档含 systemd unit 示例、nginx 反代示例、全部环境变量说明
- [ ] 文档含首次启动初始化顺序与凭证安全约定
- [ ] 干净环境按文档操作能跑通:启动 → 登录 → 录 Hub → 看到探测与状态

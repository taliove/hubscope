# 93 — 停用套件硬删迁移 + W2 例外 ADR

**What to build:** 按 spec 0014 实现决策 B。行为:随 `store.Open` 迁移机制执行一次性**幂等**数据清理——删除所有 `enabled = 0` 的 suites,连带其 cases、引用这些 suite 的 eval_runs、这些 runs 的 eval_results,顺序从叶到根(results → runs → cases → suites),在单事务内完成。迁移可重复执行(重复 Open 无副作用)。落地后:/admin 题库 tab 不再出现四个 pre-v3 旧版套件(「旧版套件」tag 与「已停用」标记随之消失);「旧版套件」筛选选项在 legacy 套件清空后顺手移除;历史批次报告读取时失去对应维度列、总分按剩余维度重算(spec 决策内行为,不做兼容)。迁移按「enabled=0 即删」通用实现——ticket 99 停用 v3 后由同一迁移自然清除,不写第二段删除代码。同时提交 **ADR:停用套件硬删(W2 数据只加不删的例外登记)**,写明四问(为什么必须改/影响哪些调用方/有无替代方案/回归测试什么)。**TDD at W1**:预置含停用套件 + 关联 runs/results 的库,经管理 API 与历史报告接口断言。

**Blocked by:** 无 — 可立即开工

**Status:** ready-for-agent

## 验收清单

- [ ] 预置停用套件 + cases + runs + results 的库,迁移后管理 API 不再列出该套件,题库列表无「旧版套件」
- [ ] 关联 eval_results/eval_runs/cases 一并删除(SQL 层无孤儿——经报告/运营历史接口观察不到幽灵记录)
- [ ] 引用被删套件的历史批次报告正常渲染:失去对应维度列,总分按剩余维度重算,不 500
- [ ] 迁移幂等:重复 store.Open 无报错、无二次删除副作用
- [ ] 启用套件及其数据零影响(回归)
- [ ] ADR 落盘 docs/adr/ 并登记 W2 例外;`make test` 全绿;check 三维度 PASS

## 风险登记

1. **不可逆**:删除即历史改写——spec 决策 Q6 已经用户明确确认(选项 A),执行不再二次确认,但 ADR 必须同票落盘
2. **事务完整性**:半删状态(如删了 suite 没删 runs)会产生幽灵数据,必须单事务
3. **W2 单连接**:迁移在 Open 路径执行,注意与既有迁移机制的串行语义一致
4. **范围边界**:只删 enabled=0 的套件;启用套件内单个停用 case 不动(spec 范围外声明)

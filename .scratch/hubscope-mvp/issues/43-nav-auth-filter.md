# 43 — 导航按登录态过滤

**What to build:** 未登录访客的顶部导航只渲染公开入口(「状态总览」)与登录按钮;会被路由门禁弹走的入口(评估、任务、管理视图)不再渲染,而不是点了才被弹去 /login。登录后导航恢复全量。登录态随路由切换重检(沿用 AppHeader 既有 refreshAuth watch 模式,不写死 mount 一次性判断)。路由级 bounce 保留为直接访问 URL 的兜底。

**Blocked by:** None — can start immediately

**Status:** done

- [ ] 未登录:导航只显示「状态总览」,右侧只显示登录按钮(「管理视图」按钮也不渲染)
- [ ] 登录后:导航显示全部项(状态总览 / 评估中心 / 任务中心 / 管理视图)
- [ ] 退出登录后导航立即回收为公开集
- [ ] 登录态判断随路由切换刷新
- [ ] 未登录直接访问 /eval、/tasks、/admin 仍被 bounce 到 /login(现有行为保留)

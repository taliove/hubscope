# 102 — 管理台设置页(SettingsPanel)布局重构

**What to build:** 重构 /admin「告警与评估设置」面板布局。用户实机反馈「太丑了」(2026-07-28 截图证据),问题清单:① 「发送测试」按钮从输入框行尾掉到下一行;② 表单项 100% 宽拉满整卡,输入框过宽、密度低;③ 评估集权重每套件一行竖排堆叠,占地且不齐;④ 近期告警事件表疑似横向溢出——「类型」列被裁切(截图中只剩「试」字残边),违反 ui-guidelines §4「卡片内容不得溢出」;⑤ 告警配置/评估配置/告警事件混在一个卡片,无分组。开工前必经 **plan agent UI 设计评审**(新布局 = 新交互形态,评审产出回写票正文;涉及新约定回写 ui-guidelines)。预期方向(以评审为准):分区卡片或分组标题(告警设置 / 评估设置)、输入控件限宽、测试按钮与输入框同行(flex,按钮 flex:none)、权重项横排 flex-wrap 紧凑排布、事件表列宽重分配杜绝溢出。管理台 12px 密度档与令牌纪律不变。

**Blocked by:** 无 — 可立即开工(设计评审先行)

**Status:** in_progress(2026-07-28 开工;待 plan agent 设计评审)

## 验收清单

- [ ] 「发送测试」与 webhook 输入框同行,不折行;请求中 loading + 禁用不变
- [ ] 表单输入控件限宽(评审定档),不再 100% 拉满;深色/浅色双主题下无溢出
- [ ] 权重项紧凑横排,标签 + input-number 成组,换行整齐
- [ ] 告警事件表「类型/内容/发送/时间」四列全部可见,卡片无横向滚动条(§4)
- [ ] 分区结构经设计评审确认;typecheck/build/vitest 全绿;check 三维度 PASS

## 风险登记

1. **§4 溢出回归**:事件表内容列长文本(错误摘要)必须截断 + tooltip,禁止撑破卡片
2. **双主题**:布局改动只消费语义令牌,暗色下抽查(密度档/描边/占位色)
3. **行为零变更**:纯布局重构——表单字段、保存逻辑、测试按钮行为、事件表数据源一律不动,vitest 既有用例应零修改通过
4. **评审前置**:未经 plan 评审的布局方案不得直接实现,避免返工

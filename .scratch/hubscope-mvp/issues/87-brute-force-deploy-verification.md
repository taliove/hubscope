# 87 — 部署面验收:两条线实测真实 IP 与 per-IP 隔离

**What to build:** spec 0011 的部署面验收(spec 测试决策末节),把 84/85/86 的改动真实生效到运行环境并实测闭环。**需要用户授权部署动作**(生产/测试线部署纪律,AGENTS.md)。步骤:① 走 deploy.sh 路径部署含本批改动的版本(测试线先行,生产线随后;版本/tag 由用户定);② 生产线实测:Caddy 配置已含替换语义 XFF、容器环境 `TRUST_PROXY=true` 生效;③ 制造数次失败登录,查审计记录显示**真实客户端 IP**(不再是 Caddy/反代地址);④ per-IP 隔离实测:来源 A 打满登录限流(429),来源 B 登录不受影响(修前是全站共享一桶,A 会把 B 挤掉);⑤ 渐进延迟实测:同一用户名连续失败,第 5 次起响应明显变慢;正确密码立即放行;⑥ 爆破告警实测:压过阈值后 Lark 群收到恰好一条告警,冷却期内不重复;⑦ 全部实测证据(命令输出/截图)归档 `.scratch/`(不进公开仓库,spec 0008 脱敏纪律)。

**Blocked by:** 84(模板与脚本修正)、85(渐进延迟)、86(爆破告警)

**Status:** ready-for-agent(部署动作需用户在场授权)

## 执行顺序

1. **测试线**:deploy.sh 部署 → 项 ②③④⑤ 实测(测试线若 84 结论为「无反代」,则 ③④ 改为验证「未注入 TRUST_PROXY、审计 IP 为直连来源」)
2. **生产线**:用户授权后 deploy.sh tag 部署 → 项 ②③④⑤⑥ 实测
3. **归档**:实测证据存 `.scratch/`;spec 0011 标 accepted;本票关闭

## 验收清单

- [ ] 生产线容器环境含 `TRUST_PROXY=true`;Caddy 站点块含 `header_up X-Forwarded-For {remote_host}`
- [ ] 失败登录的审计记录显示真实客户端 IP(两条线按各自拓扑核对)
- [ ] 来源 A 触发 429 时,来源 B 登录正常(per-IP 隔离恢复)
- [ ] 同一用户名失败满 5 次后,自第 6 次失败起响应退避变慢;正确密码立即放行
- [ ] Lark 群收到恰好一条爆破告警,冷却期内无重复;告警内容无密码
- [ ] 测试线按 80 的实测结论验证(无反代则验证未注入)
- [ ] 实测证据归档 `.scratch/`;spec 0011 Status → accepted

## 风险登记

1. **生产部署窗口**:部署即中断监控数十秒(换容器),由用户选窗口;回滚路径沿用 deploy.sh rollback
2. **存量 Caddyfile 迁移**:84 的重写路径首次在真机上跑,先备份再改,Caddy reload 失败立即还原
3. **实测制造失败登录**:会污染审计与告警——用专用测试账号或明确可识别的假用户名,避免与真实攻击混淆;Lark 告警实测提前在群里打招呼

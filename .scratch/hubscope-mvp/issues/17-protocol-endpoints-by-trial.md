# 17 — 按试通建协议端点 + 无效端点清理 + 代理固化

**What to build:** ①协议端点改为"试通才建":自动发现新模型时双协议试探,只为通的协议建端点(全不通的模型仍登记但无端点);每次同步对存量模型缺失的协议补探,上游后开通的协议自动补建;试探失败原因写同步日志,不再产生永久停用端点。手动添加模型同样先试通:至少一个协议通才创建(只建通的),全不通返回 400 不建。②管理页加"清理无效端点"按钮:删除所有"从未探测成功且当前停用"的端点及其全部历史(探测/rollup/水位/告警),返回清理数量,操作入审计。③代理固化:hubclient 显式 Transport(ProxyFromEnvironment + 拨号超时),启动日志打印生效代理(脱敏 userinfo),部署文档注明 HTTPS_PROXY 用法。

**Blocked by:** 无

**Status:** done

- [x] 发现的模型只建试通协议的端点;全不通登记但无端点;后续同步能补建后开通的协议
- [x] 手动添加模型按试通结果建端点,全不通返回 400 且不产生任何记录
- [x] "清理无效端点"按钮删除从未成功的停用端点及历史,数量可见,操作入审计
- [x] 启动日志可见生效代理;HTTPS_PROXY 环境下探测走代理成功
- [x] 黑盒测试:stub 按协议控制成败,经 API 断言端点建/不建/补建、400、清理数量

# Hub 凭证存数据库、由管理后台维护,而非配置文件

Hub 实例(base URL、API token)不按惯例放进 config.yaml / 环境变量,而是存在 SQLite 中,通过管理后台 CRUD,支持多 Hub 实例。原因:Hub 是运营对象而非部署配置——增删 Hub、换 token 属于日常操作,不该要求重启服务或改部署文件;多 Hub 支持也自然由此得来。配置文件只保留真正的部署级项(监听端口、管理员口令、数据文件路径)。代价:token 落库,需要 UI 脱敏展示、接口不回传明文。

# Task 作为一等实体承载任务中心

任务中心需要"所有低频后台作业的历史日志与运行记录"。选择新建通用 `tasks` + `task_logs` 表,让 Eval Run、发现同步、rollup 清理等都注册为 Task,统一状态机(pending/running/success/failed)与逐行日志;备选是任务中心只做对既有 eval_runs、审计日志等的联合查询视图——但那样"逐行执行日志"无处存放,各作业状态口径需逐一对齐。高频探测轮次明确不做 Task(量级会淹没任务中心,且已有专属视图)。代价:执行路径上多处要写任务状态与日志;换来新增后台作业自动进入任务中心。
